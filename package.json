{
  "name": "spike",
  "version": "1.0.1",
  "description": "Une app Electron qui fait courir un chien pixel en surcouche",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack-win": "electron-packager . spike --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "devDependencies": {
    "electron": "^25.0.0",
    "electron-packager": "^17.1.0"
  }
}